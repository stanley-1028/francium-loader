apply plugin: 'java'

dependencies {
    // No external dependencies — uses JMX and standard Java APIs

    // Logging
    implementation 'org.slf4j:slf4j-api:2.0.18'
    runtimeOnly 'ch.qos.logback:logback-classic:1.5.32'
}
